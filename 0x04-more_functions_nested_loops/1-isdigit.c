#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: int to be located
 * Return: 1 if c is a digit, else Return 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
